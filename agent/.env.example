# PM Copilot A2A Agent Configuration
# ======================================
#
# Поддерживаются два режима работы:
# 1. Локальная разработка / Self-hosted - используйте переменные OPENAI_*, A2A_*, MCP_SERVER_*
# 2. Cloud.ru Hosting - используйте переменные LLM_*, URL_AGENT, MCP_URL, AGENT_*
#
# Агент автоматически определяет режим по наличию переменных (облачные в приоритете).

# =============================================================================
# LLM Configuration
# =============================================================================

# --- Локальная разработка / Self-hosted ---
# OpenAI API key (REQUIRED)
# Получить ключ: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# OpenAI base URL (OPTIONAL)
# Используйте для кастомных OpenAI-совместимых эндпоинтов
# По умолчанию: официальный OpenAI API
# OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI model (OPTIONAL)
# Модель для reasoning агента
# По умолчанию: gpt-4o-mini
# Альтернативы: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_BASE_MODEL=gpt-4o-mini

# --- Cloud.ru Hosting (автоматически прокидываются платформой) ---
# Если эти переменные присутствуют, они имеют приоритет над OPENAI_*
#
# LLM_API_KEY - API-ключ для Foundation Models
# LLM_API_BASE - Базовый URL (https://foundation-models.api.cloud.ru/v1)
# LLM_MODEL - Модель с префиксом hosted_vllm/ (например: hosted_vllm/Qwen/Qwen3-Coder-480B-A35B-Instruct)

# Max iterations (OPTIONAL)
# Максимальное количество итераций агента
# По умолчанию: 10, диапазон: 1-50
# MAX_ITERATIONS=10

# =============================================================================
# MCP Server Connection
# =============================================================================

# --- Локальная разработка / Self-hosted ---
# URL MCP сервера
# По умолчанию: http://mcp-server:8000/mcp (Docker Compose service name)
# Для локальной разработки: http://localhost:8000/mcp
MCP_SERVER_URL=http://mcp-server:8000/mcp

# --- Cloud.ru Hosting (автоматически прокидывается платформой) ---
# MCP_URL - Список URL подключенных MCP-серверов (через запятую)
# Агент автоматически использует первый URL из списка
# Пример: http://mcp-server1:8000/mcp,http://mcp-server2:8000/mcp

# =============================================================================
# A2A Server Configuration
# =============================================================================

# A2A server host (OPTIONAL)
# По умолчанию: 0.0.0.0 (слушать на всех интерфейсах)
# A2A_SERVER_HOST=0.0.0.0

# A2A server port (OPTIONAL)
# По умолчанию: 8001
# A2A_SERVER_PORT=8001

# --- Локальная разработка / Self-hosted ---
# URL агента для Agent Card (используется клиентами для подключения)
# По умолчанию: http://localhost:8001
# Для production: укажите публичный URL (например, https://pm-copilot.example.com)
A2A_SERVER_BASE_URL=http://localhost:8001

# --- Cloud.ru Hosting (автоматически прокидывается платформой) ---
# URL_AGENT - Автоматически генерируемый URL агента
# Формат: https://{{.AgentID}}-agent.ai-agent.inference.cloud.ru

# =============================================================================
# Agent Card Metadata
# =============================================================================

# --- Cloud.ru Hosting (автоматически прокидываются платформой) ---
# Эти переменные устанавливаются платформой Cloud.ru при хостинге агента
#
# AGENT_NAME - Название агента (по умолчанию: "PM Copilot Agent")
# AGENT_DESCRIPTION - Описание агента (по умолчанию: краткое описание функций PM Copilot)
# AGENT_VERSION - Версия агента (по умолчанию: "v1.0.0")

# --- Локальная разработка / Self-hosted ---
# Для локального запуска используются defaults из кода (можно переопределить):
# AGENT_NAME=PM Copilot Agent
# AGENT_DESCRIPTION=AI-powered assistant for project managers. Automates sprint planning, status reporting, meeting coordination, and team workload management.
# AGENT_VERSION=v1.0.0

